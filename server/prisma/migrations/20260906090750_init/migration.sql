-- Identity and matching tables are applied by server/db/migrations
-- (001-005). This Prisma migration is a no-op so `prisma migrate deploy`
-- does not create a second "User" table beside the existing `users` table.
SELECT 1;
